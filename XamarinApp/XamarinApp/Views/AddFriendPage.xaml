<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.Views.AddFriendPage">
    <ContentPage.Content>
        <StackLayout
            BackgroundColor="{StaticResource PrimaryBackgroundColor}"
            Padding="20">
            <Label Text="Add friend" 
                   FontSize="Large"
                   HorizontalOptions="CenterAndExpand"
                   Margin="20"/>
            <Entry Placeholder="Type friend username in" Text="{Binding EntryUserName}" Margin="20"/>
            <Button Margin="20" Text="Confirm" BackgroundColor="{StaticResource ThirdBackgroundColor}" Command="{Binding SendRequestCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>